# Core Web Framework
fastapi
uvicorn[standard] # ASGI server for FastAPI, [standard] includes websockets and http-tools

# Database - PostgreSQL
asyncpg # For asynchronous operations with SQLAlchemy

# ORM
sqlalchemy[asyncio] # For async support with SQLAlchemy 2.0+
alembic # For database migrations

# Caching - Redis
redis

# Task Queue - Celery
celery
# flower # Optional: for monitoring Celery tasks

# AI & LangChain
langchain
# We'll add specific LangChain integrations like langchain-openai, langchain-deepseek as needed

# Pydantic & Settings
pydantic[email] # Includes pydantic itself and email validation extras
pydantic-settings # Utility for Pydantic settings with FastAPI

# Authentication & Security
passlib[bcrypt]  # For password hashing
python-jose[cryptography] # For JWT handling

# Environment Variable Management
python-dotenv

# For richer CLI output during development (optional but nice)
# rich

# For pgvector (if you install the python client for it, though not strictly needed for just using the extension via raw SQL/SQLAlchemy)
# psycopg2-binary # Sometimes needed by pgvector client libraries, or if you have sync parts. asyncpg is our main driver.
# pgvector # The Python client library for pgvector (optional, if you use its specific client features)